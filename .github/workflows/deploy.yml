# GENERATED BY STRATA — DO NOT EDIT BY HAND.
# stack:  strata-deploy-sandbox/prod/prod
#
# Strata provisioned Artifact Registry + Workload Identity Federation for this repository.
# This workflow builds your image and pushes it KEYLESSLY: GitHub mints a short-lived OIDC
# token describing this run, Workload Identity Federation exchanges it for a ~1h GCP token,
# and that token impersonates the CI service account. No long-lived key is stored anywhere.

name: Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write # required for the OIDC token GCP federates against

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # Keyless. `service_account` is required: the repo-scoped principalSet holds
      # roles/iam.workloadIdentityUser on this account, and the account is what holds
      # roles/artifactregistry.writer on the repository below.
      - uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: "projects/321829037605/locations/global/workloadIdentityPools/prod-ci-sv3qv5k6/providers/prod-gh-sv3qv5k6"
          service_account: "prod-ci-sv3qv5k6@strata-sandbox-505217.iam.gserviceaccount.com"

      - uses: google-github-actions/setup-gcloud@v3

      - run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      # ONE tag, and it is IMMUTABLE. `sha-<commit>` is the shape the composer's own
      # `values.image` names (src/server/compose/shapes.ts), so the image CI pushes is exactly
      # the image a stack can be pinned to. Nothing floating is published: a mutable tag
      # in the registry is a pointer a deploy can be aimed at, and that defeats the pinning —
      # so no floating tag is published alongside it.
      - name: Build and push
        env:
          IMAGE: "us-central1-docker.pkg.dev/strata-sandbox-505217/prod-images/demo"
        run: |
          docker build -t "$IMAGE:sha-${{ github.sha }}" .
          docker push "$IMAGE:sha-${{ github.sha }}"
